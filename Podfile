# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Messenger Application' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Messenger Application
   pod 'Firebase/Core' 

   pod 'Firebase/Auth' 

   pod 'Firebase/Database'

   pod 'Firebase/Storage'
   
   # Facebook
   pod 'FBSDKLoginKit'

   # Google Log In 
   pod 'GoogleSignIn'

  pod 'MessageKit'
  pod 'JGProgressHUD' # spinner overlay (heads up display)
  pod 'RealmSwift' # similar to coredata (save to device) optimize
  pod 'SDWebImage' # Image load capability (caching)
  pod 'InputBarAccessoryView'
   

end
